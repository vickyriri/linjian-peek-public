name: Build Android Debug APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'android/**'
      - '.github/workflows/android-debug.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platform-tools platforms;android-34 build-tools;34.0.0'

      - name: Test usage timeline
        run: |
          mkdir -p android/build/unit-tests
          javac -encoding UTF-8 -source 11 -target 11 \
            -d android/build/unit-tests \
            android/app/src/main/java/dev/linjian/peek/UsageTimeline.java \
            android/tests/dev/linjian/peek/UsageTimelineTest.java
          java -cp android/build/unit-tests dev.linjian.peek.UsageTimelineTest

      - name: Build APK
        run: bash android/build.sh

      - name: Verify APK exists
        run: |
          ls -lh android/*.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: zhangxinchuang-public-debug-apk
          path: android/*.apk
          if-no-files-found: error
